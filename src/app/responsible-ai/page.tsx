import TrustPage from "@/app/trust/page";

export const metadata = {
  title: "Trust, Governance & Boundaries — Namuste",
  description: "How Namuste operates with strict domain boundaries, verifiable data governance, PII masking, and deterministic human escalation.",
};

export default function ResponsibleAIRedirect() {
  return <TrustPage />;
}
